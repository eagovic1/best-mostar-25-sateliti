import { DateToModelPipe } from './date-to-model.pipe';

describe('DateToModelPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToModelPipe();
    expect(pipe).toBeTruthy();
  });
});
